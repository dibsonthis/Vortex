// import [PI, e] : "../imports/math";

// var person = {
// 	name: "John",
// 	age: 23
// }

// person.name::onChange = (curr, old) => {
// 	print("Name was changed from " + old + " to " + curr)
// }

var person = {
	name: "John"
}

person.name::onChange = (curr, old) => {
	println("name changed from " + string(old) + " to " + string(curr))
}
person::onChange = (curr, old) => {
	println("person changed from " + string(old) + " to " + string(curr))
}

person = "Allan"