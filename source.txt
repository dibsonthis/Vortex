// import [PI, e] : "../imports/math";

// var person = {
// 	name: "John",
// 	age: 23
// }

// person.name::onChange = (curr, old) => {
// 	print("Name was changed from " + old + " to " + curr)
// }

var lexer = {
	index: 0,
	tokens: ["const", "x", "=", "5"],
	currentNode: this.tokens[this.index]
}

lexer.index::onChange = (curr) => {
	lexer.currentNode = lexer.tokens[curr]
}

println(lexer)
lexer.index = 3
println(lexer)
println(4.5323)