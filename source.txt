// import [PI, e] : "../imports/math";

var xPos = 0
var yPos = 10

[xPos, yPos]::onChange = () => {
	if (xPos < yPos) {
		print("xPos < yPos\n")
	} else if (xPos == yPos) {
		print("xPos == yPos\n")
	} else {
		print("xPos > yPos\n")
	}
}

for (1..15) {
	xPos = xPos + 1
}