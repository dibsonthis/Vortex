import [initSDL, createWindow, eventLoop] : "./import_test"

var init = initSDL()
if (init != 0) {
	println("Uh oh, error")
} else {
	println("Initialized SDL")
}

const window = createWindow("Test Window", 0, 0, 600, 400)

eventLoop(window)