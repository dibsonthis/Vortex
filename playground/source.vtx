type KeyOf[T: Object] = T.keys() as Union

type ValueOf[T: Object] = T.values() as Union

type Bloop = (T: Object) => {
    ret (T: T, K: KeyOf(T)) => ValueOf(T)
}

const obj = {
    name: "Hi",
    age: 34
}

println(Bloop(obj).return)