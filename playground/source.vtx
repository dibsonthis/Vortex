type Obj {
    name: String,
    age: String
}

type Partial(T) {
	var partial = {}

	for (T.keys, i, k) {
		partial[k] = union _ {T[k], None}
	}

	partial
}

type PartialObj = Partial(Obj)

print(PartialObj)