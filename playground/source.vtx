type Partial(T) {
	var partial = {}

	for (T.keys, i, k) {
		partial[k] = union _ {T[k], None}
	}

	partial as Type
}

type Obj {
    name: String,
    age: Number
}

type PartialObj = Partial(Obj)

const p: PartialObj = PartialObj {
    name: None,
    age: 56
}