// import [] : string

// var blah = [(x: String) => x.chars(), (x: Number) => Number]

// print(test(blah))

type Person {
    name: String,
    age: Number,
    address: {
        street: String,
        postcode: Number
    }
}

const p: Person = Person {
    name: "Allan", 
    age: 34, 
    address: {
        street: "123 Fake st.",
        postcode: 1234
}}