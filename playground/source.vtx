const start = clock()

const g = 100

const outer = () => {
    var g = g
    const inner = () => {
        g += 200
        return g
    }
    print(g, "\n")
    return inner
}

const inner = outer()
print(inner(), "\n")
print(inner(), "\n")

print("Duration: ", clock() - start)

/*
    TODO

    - Add nested closures
    - Add Objects
    - Add Imports
*/