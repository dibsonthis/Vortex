type Record(Keys, Type) {
    var obj = {}
    for (Keys, i, k) {
        obj[string(k)] = Type
    }
    obj
}

const Ids = [1001, 1002, 1003]

type User {
    name: String,
    age: Number
}

print(Record(Ids, User))
