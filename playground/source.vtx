import websockets : "/Users/adib/Dev/Personal/Vortex/Modules/modules/websockets/websockets"
import [] : Future
import time

const s = websockets.server()
Future(() => websockets.server_listen(s, 9002))

while (true) {
    time.sleep(5000)
    websockets.server_send(s, "hello world")
}