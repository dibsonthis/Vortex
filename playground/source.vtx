import [println] : "time"

const coroutine = (...args) => {
    println(args)
    var output = 0
    while (true) {
        yield output += 1
    }
}

dis(coroutine)

const gen = coroutine()
gen()
gen()