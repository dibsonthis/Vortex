const start = clock()

const outer = (x) => {
    const inner = () => print(x)
    inner()
    return x
}

outer(45)

print("Duration: ", clock() - start)

/*
    TODO

    - Add SET_CLOSURE
    - Add nested closures
    - Add Objects
    - Add Imports
*/