import os

const printDirs = (path: String) => {
	for (os.listDir(path), index, item) {
		if (item.isDir) {
			println(item.filePath)
			printDirs(item.filePath)
		}
	}
}

printDirs(".")
println(os.osName())