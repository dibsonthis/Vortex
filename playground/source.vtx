type Implements(T, FunctionName, F) {
    for (T.keys, index, key) {
        if (key == FunctionName) {
            if (T[key] is F) {
                ret true
            }
        }
    }

    error(f"${T} does not implement ${FunctionName}")
}

type Blah {
    name: String,
    getName: () => String
}

type Person {
    name: String,
    age: Number
}

const person = {
    name: "John",
    getName: () => this.name
}

print("here")

Person.Implements("getName", () => String)