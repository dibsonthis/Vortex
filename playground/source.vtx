// const Timer = (func) => {
//     return (...args) => {
//         const start = clock()
//         var value = func(...args)
//         const duration = clock() - start
//         print(f"Duration (${func.info().name}): ", duration, "\n")
//         return value
//     }
// }

const Wrapper = (count, func) => {
    return (...args) => {
        print(count, "\n")
        return func(...args)
    }
}

@[Wrapper(10), Wrapper(100)]
const func = (a, b) => a + b

print(func(10, 20))