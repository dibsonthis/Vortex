type KeyOf(T) {
    ret T.keys as Union
}

type Person {
    name: String,
    age: Number,
    height: Number
}

const get = (key: KeyOf(Person), p: Person) => {
    p[key as key[0]]
}

const person = Person {name: "Steven", age: 32, height: 180}

print(get("ages", person))