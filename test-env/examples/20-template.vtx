import io

const loadTemplate = (filePath: String) => {
	var template = io.readf(filePath)
	template = "f\"" + template.replaceAll("\"", "\\\"") + "\""
	template.replaceAll("\n", "\\n")
}

const renderTemplate = (template: String, data: Object) => {
	var rendered = eval(template)
}

const template = loadTemplate("data/template.html")

const data = {
	name: "John",
	age: 34,
	nums: [1, 2, 3, 4]
}

const renderedTemplate = renderTemplate(template, data)

print(template)
print(renderedTemplate)
