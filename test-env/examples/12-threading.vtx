import threading
import [] : threading
import requests

const data = threading.thread(() => requests.get("https://24pullrequests.com/projects.json"))

while (!data.ready()) {
    print("Loading...\r")
}

const res = data.get()

println(res.toJson())